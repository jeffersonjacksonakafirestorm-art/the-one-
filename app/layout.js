export const metadata = {
  title: 'CallRecoverAI — Stop Losing Jobs to Missed Calls',
  description: 'AI instantly texts back every missed caller. Qualifying leads and booking jobs automatically.',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
